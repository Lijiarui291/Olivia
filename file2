/*#include <stdio.h>

int main()
{
	//int x, y;
	//4 * x + 5 * y = 9;（左边不能用表达式 ） 
	//x + 7 * y = 13;
	//printf("x = %d,y = %d\n", x, y);
	//return 0;
	//(ax + by = c)
	//(mx + ny = p) x = (c * n - b * p)/(a * n -b * m) y=(c * m - a * p)/(b * m - a * n)
	
    /*int x, y;
	int a, b, c, m, n, p;
	printf("请输入二元一次方程的系数：");
	scanf("%d %d %d %d %d %d", &a, &b, &c, &m, &n, &p);
	x = (c * n - b * p)/(a * n -b * m);
	y=(c * m - a * p)/(b * m - a * n);
	printf("x值为%d,y值为%d\n", x, y);
	return 0;
}*/
	/*#include <stdio.h>
	int main();
	{
		int height, length, width, volume, weight;
		
		height = 8;
		length = 12;
		width = 10;
		scanf("%d%d%d, &height, &length, &width");
		printf("%d,%d,%d\n, height, length, width");
		volume = height * length * width;
		weight = (volume + 165)/ 166;
		
		printf("Dimensions: %d         xxx%dx%d\n", length, width, height);
		printf("Volume (cubic inches): %d\n, volume");
		printf("Dimensional weight (pounds): %d\n", weight);
		return 0;
	}*/
	/*#include <stdio.h>
	int main(void)
	{
		int height, length, width, volume, weight;
		
		printf("Enter height of box:");
		scanf("%d, &height");
		printf("Enter length of box:");
		scanf("%d, &length");
		printf("Enter width of box:");
		scanf("%d, &width");
		volume = height * length * width;
		weight = (volume + 165) / 166;
		
		printf("Volume (cubic inches): %d\n", volume);
		printf("Dimensional weight (pounds): %d\n, weight");
		return 0;
	}*/
	 
#include <stdio.h>
#define FREEZING_PT 32.0f
#define SCALE_FACTOR (5.0f / 9.0f) 
 
int main(void)
{
	float fahrenheit, celsius;
	printf("Enter Fahrenheit temperature:");
	scanf("%f" , &fahrenheit);
	
	celsius = (fahrenheit - FREEZING_PT) * SCALE_FACTOR;
	
	printf("Celsius equivalent: %.1f\n", celsius);//这里刚开始的时候把后引号打到celsius后面了，导致输出错误 
	
	return 0;
}





/*#include <stdio.h>

#define FREEZING_PT 32.0f
#define SCALE_FACTOR (5.0f / 9.0f)

int main(void)
{
	float fahrenheit, celsius;
	
	printf("Enter Fahrenheit temperature:");
	scanf("%f", &fahrenheit);
	
	celsius = (fahrenheit - FREEZING_PT) * SCALE_FACTOR;
	
	printf("Celsius equivalent: %.1f\n", celsius);
	
	return 0; 
 } */
