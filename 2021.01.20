//洛谷 循环结构p5762
/*#include<iostream>
#include<math.h>
#include<algorithm>
#include<stdio.h>
using namespace std;

int main() {
	int N;
	cin >> N;
	double fenshu[10001];
	for (int i = 0;i < N;i++) {
		cin >> fenshu[i];
	}
	sort(fenshu, fenshu + N);
	double sum = 0;
	for (int i = 1;i < N - 1;i++) {
		sum += fenshu[i];
	}
	//cout << sum;
	double ave;
	ave = sum / (N - 2);
	printf("%.2f", ave);
	
	return 0;
}*/


//洛谷 字符串p1321
/*#include<iostream>
#include<string.h>
#include<string>
using namespace std;

int main() {
	int cntboy = 0;
	int cntgirl = 0;
	char zifuchuan[256];
	cin >> zifuchuan;
	int len = strlen(zifuchuan);
	for (int i = 0;i < len;i++) {
		if (zifuchuan[i] == 98 || zifuchuan[i + 1] == 111 || zifuchuan[i + 2] == 121) {
			cntboy++;
		}
		if (zifuchuan[i] == 103 || zifuchuan[i + 1] == 105 || zifuchuan[i + 2] == 114 || zifuchuan[i + 3] == 108) {
			cntgirl++;
		}
	}
	cout << cntboy << endl;
	cout << cntgirl;
	return 0;
}*/
