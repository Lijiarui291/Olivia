//洛谷 循环结构p5762
/*#include<iostream>
#include<math.h>
#include<algorithm>
#include<stdio.h>
using namespace std;

int main() {
	int N;
	cin >> N;
	double fenshu[10001];
	for (int i = 0;i < N;i++) {
		cin >> fenshu[i];
	}
	sort(fenshu, fenshu + N);
	double sum = 0;
	for (int i = 1;i < N - 1;i++) {
		sum += fenshu[i];
	}
	//cout << sum;
	double ave;
	ave = sum / (N - 2);
	printf("%.2f", ave);
	
	return 0;
}*/
