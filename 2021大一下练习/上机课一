#include<iostream>
#include<algorithm>
using namespace std;

int main() {
	int k;
	cin >> k;
	int num[11];
	while (k--) {
		for (int i = 0;i < 10;i++) {
			cin >> num[i];
		}
		reverse(num, num + 10);
		for (int i = 0;i < 9;i++) {
			cout << num[i] << " ";
		}
		cout << num[9] << endl;
	}
	
	return 0;                                                         

}


#include<iostream>
#include<string.h>
using namespace std;
string yuanzifu;
string add;
void mubiao(string yuanzifu, string add) {
	int len = yuanzifu.length();
	for (int i = 0;i < len;i++) {
		cout << yuanzifu[i] + add;
	}
}

int main() {
	cin >> yuanzifu;
	cin >> add;
	mubiao(yuanzifu, add);

	return 0;
}
