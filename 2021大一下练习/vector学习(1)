#include<iostream>
#include<vector>
using namespace std;
using std::vector;

int main() {
	int num;
	vector<int> NUM;
	for (int i = 0;i < 8;++i) {
		cin >> num;
		NUM.push_back(num);
	}
	for (auto& i : NUM) {//i是NUM的引用
		cout << i << " ";//输出容器中的每个元素值
	}
	return 0;
}
