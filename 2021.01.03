//B: 不同的元素
//给定一个整型数组，统计不同的元素的个数
//每组输入占一行，每先给出正整数n（≤30），随后是n个整数，以空格分隔
//输出占一行,以回车结束。输出不同的元素的个数
/*#include <iostream>
using namespace std;

int main(){
	//输入数字后建立数组
	//建立新的数组，把不同的存入新数组，计算个数
	int n;
	cin>>n;
	int num[32];
	int i=0;
	for(i=0;i<n;i++){
		cin>>num[i];
	}
	
	int j=0;
	int pos=0;

	int cnt=n;
	for(i=0;i<n;i++){
		for(j=0;j<pos;j++){
			if (num[i]==num[j]){
			cnt--;
		    break;
			}
		}
		pos++;
	    //cout<<j<<endl;
	}
	
	cout<<cnt<<endl;
	
	return 0;
	
}*/

